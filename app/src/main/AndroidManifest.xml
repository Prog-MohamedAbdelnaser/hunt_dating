<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          package="com.recep.hunt">

    <dist:module dist:instant="true"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
            android:name=".volleyHelper.BackendVolley"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".setupProfile.SetupProfileInterestedInActivity">
        </activity>
        <activity android:name=".setupProfile.SetupProfileLookingForActivity">
        </activity>
        <activity android:name=".setupProfile.SetupProfileGalleryActivity">
        </activity>
        <activity android:name=".setupProfile.SetupProfileGenderActivity">
        </activity>
        <activity android:name=".setupProfile.SetupProfileUploadPhotoActivity">
        </activity>
        <activity android:name=".setupProfile.SetupProfileDobActivity">
        </activity>
        <activity android:name=".setupProfile.SetupProfileActivity">
        </activity>
        <activity android:name=".login.SocialLoginActivity">
        </activity>
        <activity android:name=".login.OtpVerificationActivity">
        </activity>
        <activity android:name=".login.LoginActivity">
        </activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts"/>
    </application>

</manifest>